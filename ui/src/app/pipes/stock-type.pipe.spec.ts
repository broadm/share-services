import { StockTypePipe } from './stock-type.pipe';

describe('StockTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StockTypePipe();
    expect(pipe).toBeTruthy();
  });
});
